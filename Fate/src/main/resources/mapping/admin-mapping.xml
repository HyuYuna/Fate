<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="adminDao">

	<!-- 아이디 중복체크 -->
	 <select id="idCheck" parameterType="String" resultType="Integer">
	 	SELECT
	 		count(*)
		FROM
			USER
		WHERE 
			USER_ID = #{userId}
	 </select>
	 
	 <!-- 고객 가입 -->
	<insert id="joinUser" parameterType="userInfoVO">
	 <![CDATA[
		INSERT INTO USER
		(
			USER_ID, 
			PASSWORD, 
			USER_NAME
		) VALUES (
			#{userId},
			#{password}, 
			#{userName}
		) 	
	 ]]>
	 </insert>
	 
	 <!-- 고객 수정 -->
	 <update id="editUser" parameterType="userInfoVO">
	  <![CDATA[
		UPDATE 
			USER
		SET
			USER_ID = #{userId} ,
			PASSWORD = #{password}, 
			USER_NAME = #{userName}
		WHERE
			USER_ID = #{userId}
	 ]]>
	</update>
	 
	 
	
</mapper>